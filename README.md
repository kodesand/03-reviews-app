- react icons
- local data

```
const reviews = [
    {},{},{}
]

export default reviews
```

access the array
destructure array
`const { name, job, image, text } = people[index]`

Carousel

- previous button
- next button
- random button
